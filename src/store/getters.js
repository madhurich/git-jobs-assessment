const jobs = state => state.jobs;
const noResults = state => state.noResults;
const nightMode = state => state.nightMode;
const isFailure = state => state.isFailure;

export default {
    jobs,
    noResults,
    nightMode,
    isFailure,
}